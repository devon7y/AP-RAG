# Dependencies & environments
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# LightRAG has its own repo
LightRAG/

# RAG storage data (large JSON/binary files)
westbury/rag_storage_westbury_qwen3_32b/

# OS files
.DS_Store

# Local-only / not part of the published wrapper
.claude/
# unrelated 523MB Next.js app (has node_modules)
lightrag-explainer/
# local backup artifacts
*.pre_upgrade_backup
# personal roadmap notes
new_embedding_changes.txt

# Regenerable artifacts (PDF-naming + chunk caches, produced under scripts/)
.crossref_cache.json
pdf_name_report.csv
rename_map.tsv
rename_review.tsv
.pytest_cache/

# PDF-renaming / OCR / dedup toolchain: one-off run data, caches, batch I/O (regenerable)
scripts/*.tsv
scripts/*.jsonl
scripts/*_state.json
scripts/.batch_rename_*.json
scripts/.llm_cache_*.json
scripts/.llm_rename_cache.json
scripts/.gemini_rename_cache.json
# apa-manifest build caches + smoke-test artifacts (regenerable)
.crossref_full_cache.json
.smoke_manifest.json
.apa_manifest_batch_smoke.json
*_smoke*.json
*.log
