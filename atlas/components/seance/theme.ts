/** Séance visual constants — spectral green family over the shared dark tokens. */

/** Hub accent for this experience (tag chrome, matches app/page.tsx). */
export const SEANCE_ACCENT = "#008300";

/** Brighter interactive green that stays readable on the dark surface. */
export const SEANCE_UI = "#35d17a";

/** Ghost body glow (emissive base, scaled by hdrBoost). */
export const GHOST_GREEN = "#54e08f";

/** Ghost hot core (pushed past 1.0 on HDR canvases). */
export const GHOST_HOT = "#c9ffd9";

/** Terrain height scale shared with the other map scenes. */
export const HEIGHT_SCALE = 16;
